import WorkLayout from "../../layouts/WorkLayout";

export const meta = {
  slug: "tfg",
};

# The Foschini Group

I led the development in building and maintaining the following apps between 2019 and 2021:

- myTFGWorld
- sportscene
- Foschini for Beauty (decommissioned late 2021)

export default ({ children }) => <WorkLayout {...meta}>{children}</WorkLayout>;
